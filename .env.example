# Where the Vite dev server proxies /api and /hls-proxy during `npm run dev`.
# Not needed for production builds if you serve the app from behind the same
# origin/reverse proxy as the MediaFeed API.
VITE_API_PROXY_TARGET=http://localhost:8080
